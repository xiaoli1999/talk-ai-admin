// 文档教程: https://uniapp.dcloud.net.cn/uniCloud/schema
{
  "bsonType": "object",
  "required": [],
  "permission": {
  	"read": true,
  	"create": true,
  	"update": true,
  	"delete": true
  },
  "properties": {
    "_id": {
      "description": "记录ID",
      "bsonType": "string"
    },
    "user_id": {
      "description": "填写用户ID",
      "bsonType": "string",
	  "foreignKey": "users._id"
    },
    "source": {
      "description": "页面来源",
      "bsonType": "string"
    },
    "version": {
      "description": "问卷版本(如 v1/v2)，用于多版本并存与按版本判定是否已填；历史无此字段的记录按 v1 处理",
      "bsonType": "string"
    },
    "answers": {
      "description": "问卷答案(KV结构)",
      "bsonType": "object"
    },
    "duration": {
      "description": "填写时长(秒)",
      "bsonType": "int"
    },
    "created_time": {
      "description": "提交时间",
      "bsonType": "timestamp",
      "forceDefaultValue": {
        "$env": "now"
      }
    }
  }
}
