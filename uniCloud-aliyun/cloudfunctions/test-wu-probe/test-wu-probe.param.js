// 本文件中的内容将在云对象【运行】时解析为运行参数
// 配置教程参考：https://uniapp.dcloud.net.cn/uniCloud/rundebug.html#run-obj-param
//
// 正确格式：直接写「方法名(参数)」的调用语句（不是 module.exports，也不是 {method,params}）。
// 多个调用语句时只取第一个有效。换方法就改下面那一行（把别的注释掉、解开你要的那一行）。
//
// ★ 一键测试：跑这一行就够 ★
//   顺序已改：先跑「大文档写」(pushIntoBig/setScalarInBig，各150次≈86秒，本地也跑得完)，
//   后跑读操作(只有云端运行跑得满；本地到点被截无妨，反正本地优先架构下读≈0)。
//   ⚠️ 能用「上传到云端运行」就用云端(操作~10ms能跑满7个)；用本地运行也行，这次至少能拿到全部「写」口径。
//   预期：总写WU若A(~2000)/若B(~111300)；总读RU若投影有效(~40000)/无效(~100000)。
//   返回有 window 时间 + 各写实验逐项 a(若A的WU)/b(若B的WU)。跑完去【费用中心>用量统计】截那一小时的「写WU/读RU」增量发我。
runAll()

// —— 想单独跑某个探针，就注释掉上面 runAll()、解开下面对应一行 ——
// pushIntoBig({ n: 200, bigKB: 100 })     // 最决定性 A/B：写WU ≈200→A / ≈20000→B
// pushGrowing({ n: 500, sizeKB: 2 })      // 桶不断变大时 push：~1000(A)/~250000(B)
// setScalarInBig({ n: 200, bigKB: 100 })  // 大文档改小字段：≈200(A)/≈20000(B)
// addSingles({ n: 500, sizeKB: 2 })       // 单条存写基线：≈1000
// incScalar({ n: 1000 })                  // 亲密度 inc 基线：≈1000
// readBig({ n: 500, bigKB: 100 })         // 读全量大文档：RU≈12500
// readProjected({ n: 500, bigKB: 100 })   // 投影只取小字段：远小→投影有效
// cleanup()                               // 测完清空测试表（无参数）
